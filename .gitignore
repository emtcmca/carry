node_modules/
dist/
*.log
.env
.env.local
# libSQL / SQLite local data (slice 2)
*.db
*.db-journal
*.sqlite
data/
data-test/
# Local Claude Code project config (lane agent defs, etc.) — not for the public repo
.claude/
